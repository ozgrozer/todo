# nodejs

Available commands:

Add new task:

`$ node app.js -a|--add "<taskTitle>"`

List all tasks:

`$ node app.js -l|--list`

Update task:

`$ node app.js -u|--update <taskId> "<taskTitle>"`

Delete task:

`$ node app.js -d|--delete <taskId>`

Available commands:

`$ node app.js`
